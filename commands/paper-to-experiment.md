# Command: Paper To Experiment

Use `paper-tutorial-briefing` and `experiment-idea-planner`.

Inputs:

- paper title or PDF;
- local code path;
- available GPU budget;
- baseline metric.

Output:

- shortest faithful experiment;
- metric gate;
- failure stop rule;
- README claim that would be justified if it works.
